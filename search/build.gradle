apply from: "$rootProject.projectDir/android-library.gradle"

android {
    namespace 'com.astroscoding.search'
    defaultConfig {
        consumerProguardFiles "consumer-rules.pro"
    }
}

dependencies {
    // common module
    implementation project(":common")
}